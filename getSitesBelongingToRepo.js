import "dotenv/config";

// Changes the branches of any sites using these repos with branchToChange from branchToChange to branchToChangeTo
// Details of repos and branches to change
const repo = "tomrcc/persy-porty";

(async () => {})();
